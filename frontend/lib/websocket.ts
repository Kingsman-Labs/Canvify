// WebSocket client — connects to backend /ws for live agent progress
export function connectProgressSocket(sessionId: string) {
  // TODO: new WebSocket(`${process.env.NEXT_PUBLIC_WS_URL}?session=${sessionId}`)
}
