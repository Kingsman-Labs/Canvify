// Interactive report viewer (uses components/ReportViewer.tsx, ChartDisplay.tsx,
// RiskHeatMap.tsx, InvestorScoreCard.tsx)
export default function Report({ params }: { params: { id: string } }) {
  return <main>{/* TODO: <ReportViewer reportId={params.id} /> */}</main>;
}
