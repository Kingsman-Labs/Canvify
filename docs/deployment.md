# Deployment

- Frontend: Vercel (auto-deploy on push to main via GitHub Actions)
- Backend: Railway (auto-deploy on push to main via GitHub Actions)
- See .github/workflows/ci-cd.yml for the full pipeline.
